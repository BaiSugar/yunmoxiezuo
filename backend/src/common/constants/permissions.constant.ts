/**
 * 权限常量 - 向后兼容文件
 * 
 * 注意：此文件仅用于向后兼容，实际定义在 ../config/permissions.config.ts
 * 所有权限的修改请在 permissions.config.ts 中进行
 */

export {
  USER_PERMISSIONS,
  ROLE_PERMISSIONS,
  PERMISSION_PERMISSIONS,
  NOVEL_PERMISSIONS,
  PROMPT_PERMISSIONS,
  AI_MODEL_PERMISSIONS,
  PERMISSIONS,
  type PermissionCode,
} from '../config/permissions.config';

